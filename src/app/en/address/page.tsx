// EN mirror — same content as zh counterpart.
// Real bilingual wiring (copy.en.ts → client) is wired via useT() already.
import ZhPage from '../../address/page'
export const dynamic = 'force-static'
export default ZhPage
