/**
 * copy.ko — Korean translation stub (Phase 3.6.1).
 *
 * Empty by design: useT() falls back to English copy when a key is
 * missing here. Add keys as translations get done.
 */
import type { PartialDeep } from './copy-types';
import { COPY_EN } from './copy.en';

export const COPY_KO = {} as PartialDeep<typeof COPY_EN>;
